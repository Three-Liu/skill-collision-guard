# Skill collision checks

Before installing or invoking a coding-agent skill/plugin, use the `skill-collision-guard` skill. Report conflicts and wait for the user's choice before removing or bypassing a skill. Session suppression is reversible and must not move the underlying files.

interface:
  display_name: "Skill Collision Guard"
  short_description: "Detect and suppress conflicting agent skills"
  default_prompt: "Use $skill-collision-guard only when the user explicitly asks to inspect or compare coding-agent skills before installation or invocation."

policy:
  allow_implicit_invocation: false

# Skill collision checks

Before installing or invoking a coding-agent skill/plugin, use `skills/skill-collision-guard/SKILL.md`. Report conflicts before removal or override. Session suppression must remain reversible and must not move the original files.
